#include "main.h"

/**
 * _isdigit - check if character @c is an ascii digit
 * @c: the character to check
 * Return: 1 if @c is a digit else 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
