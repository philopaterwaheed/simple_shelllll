#include "main.h"

/**
 * _isupper - check if character @c is in uppercase
 * @c: the character to check
 * Return: 1 if @c is uppercase else 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
