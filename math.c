#include "main.h"
/**
 * min - math helper function to be used in realloc
 * @a: the first number
 * @b: the seconed number
 * Return: the minmum number.
 */
int min(int a, int b)
{
	if(a < b)
		return (a);
	else
		return (b);
}
