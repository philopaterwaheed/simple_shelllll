#include "main.h"
/**
* _strlen - does some thing
* @u: ptr1
* Return: len
*/
int _strlen(char *s)
{
	int i; 
	int size = 0;
	
	while(ture)
	{
		if (*(s++) != NULL)
			size++;
		else
			break;
	}
	return (size);	
}
